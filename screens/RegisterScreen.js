import React from 'react'
import { StyleSheet, View, Text } from 'react-native'

const RegisterScreen = ({ navigation }) => {
    return (
        <View>
            <Text>on s'enregiste mgl</Text>
        </View>
    )
}

export default RegisterScreen;

const styles = StyleSheet.create({});